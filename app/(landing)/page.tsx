import { NextPage } from "next";
import { ReactElement } from "react";

const LandingPage: NextPage = (): ReactElement => {
  return <span>Ini adalah landing page</span>;
};

export default LandingPage;
